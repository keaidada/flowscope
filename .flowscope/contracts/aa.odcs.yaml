apiVersion: v3.1.0
kind: DataContract
id: new_contract
name: New Contract
version: 1.0.0
status: active

custom:
  flowscope:
    lineage_rules:
      - id: no_orphan_output
        severity: P2
    sql_rules:
      - id: no_select_star
        severity: P2
